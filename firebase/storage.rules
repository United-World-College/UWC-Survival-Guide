rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can view avatars; only the owner can upload
    match /avatars/{uid} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
